import React from 'react';
import { Box, Text } from 'ink';

export function LinkedInMain({ cli }: { cli: any }) {
	// Placeholder: parse cli input and show appropriate view
	return (
		<Box flexDirection="column">
			<Text color="blueBright">LinkedIn CLI (TypeScript) - Placeholder UI</Text>
			<Text>Use commands: profile, connections, posts, messaging</Text>
			<Text color="gray">(TypeScript implementation scaffolded. Replace with real UI.)</Text>
		</Box>
	);
}
